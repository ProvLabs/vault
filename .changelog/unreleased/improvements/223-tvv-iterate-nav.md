* Reworked `GetTVVInUnderlyingAsset` to value the accepted denoms directly and iterate the per-vault Internal NAV table for held assets instead of walking every principal-marker balance, so Total Vault Value cost scales with the number of valued denoms rather than with whatever is parked at the principal marker [#223](https://github.com/provlabs/vault/issues/223).

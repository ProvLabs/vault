* Add `MigrateInternalNAVSeedFromMarker` to seed the Internal NAV table from Marker NAV for vaults whose payment denom differs from underlying, and default `nav_authority` to the vault admin when unset. Bumps the vault module `ConsensusVersion` to 2 and registers a v1->v2 migration handler so the SDK module manager can drive the migration via `RunMigrations` during an upstream upgrade [#191](https://github.com/provlabs/vault/issues/191).

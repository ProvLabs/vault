* Added the Provenance `x/exchange` module (and its `x/hold` and `x/metadata` dependencies) to the vault module and SimApp. The vault keeper now holds an `ExchangeKeeper` reference (`GetPayment`/`AcceptPayment`/`RejectPayment`) for the upcoming P2P multi-asset workflow, and the SimApp wires the exchange, hold, and metadata keepers with real payment custom-signers so it can process exchange transactions [#192](https://github.com/provlabs/vault/issues/192).
